{
  final class $anon() extends Regex[Tuple2[String, Either[Either[String, String], Tuple2[String, String]]]] {
    override def matches(input: CharSequence): Boolean = ((`input₂`: CharSequence) => {
      val result: Int = {
        val lp: Int = if (0.<(`input₂`.length()).&&(`input₂`.charAt(0).==(97))) {
          def self(p: Int): Int = {
            val step: Int = {
              val `lp₂`: Int = if (p.<(`input₂`.length()).&&(`input₂`.charAt(p).==(97))) if (p.+(1).!=(p)) self(p.+(1)) else -1 else -1
              if (`lp₂`.>=(0)) `lp₂` else if (p.<(`input₂`.length()).&&(`input₂`.charAt(p).==(98))) if (p.+(1).!=(p)) self(p.+(1)) else -1 else -1
            }
            if (step.>=(0)) step else if (p.==(`input₂`.length())) p else -1
          }
          self(0.+(1))
        } else -1
        if (lp.>=(0)) lp else if (0.<(`input₂`.length()).&&(`input₂`.charAt(0).==(98))) {
          def `self₂`(`p₂`: Int): Int = {
            val `step₂`: Int = {
              val `lp₃`: Int = if (`p₂`.<(`input₂`.length()).&&(`input₂`.charAt(`p₂`).==(97))) if (`p₂`.+(1).!=(`p₂`)) `self₂`(`p₂`.+(1)) else -1 else -1
              if (`lp₃`.>=(0)) `lp₃` else if (`p₂`.<(`input₂`.length()).&&(`input₂`.charAt(`p₂`).==(98))) if (`p₂`.+(1).!=(`p₂`)) `self₂`(`p₂`.+(1)) else -1 else -1
            }
            if (`step₂`.>=(0)) `step₂` else if (`p₂`.==(`input₂`.length())) `p₂` else -1
          }
          `self₂`(0.+(1))
        } else -1
      }
      result.==(`input₂`.length())
    }).apply(input)
    override def matchesWithCaps(`input₃`: CharSequence): Option[Array[Int]] = ((`input₄`: CharSequence) => {
      val inputLen: Int = `input₄`.length()
      val groups: Array[Int] = Array.fill[Int](8)(-1)(ClassTag.apply[Int](classOf[Int]))
      groups.update(0, 0)
      val `result₂`: Int = {
        val savedStart: Int = groups.apply(2)
        groups.update(2, 0)
        val res: Int = {
          val `lp₄`: Int = {
            val `savedStart₂`: Int = groups.apply(4)
            groups.update(4, 0)
            val `res₂`: Int = if (0.<(`input₄`.length()).&&(`input₄`.charAt(0).==(97))) {
              val savedEnd: Int = groups.apply(5)
              groups.update(5, 0.+(1))
              val `res₃`: Int = {
                val `savedEnd₂`: Int = groups.apply(3)
                groups.update(3, 0.+(1))
                val `res₄`: Int = {
                  def `self₃`(`p₃`: Int): Int = {
                    val `step₃`: Int = {
                      val `savedStart₃`: Int = groups.apply(2)
                      groups.update(2, `p₃`)
                      val `res₅`: Int = {
                        val `lp₅`: Int = {
                          val `savedStart₄`: Int = groups.apply(4)
                          groups.update(4, `p₃`)
                          val `res₆`: Int = if (`p₃`.<(`input₄`.length()).&&(`input₄`.charAt(`p₃`).==(97))) {
                            val `savedEnd₃`: Int = groups.apply(5)
                            groups.update(5, `p₃`.+(1))
                            val `res₇`: Int = {
                              val `savedEnd₄`: Int = groups.apply(3)
                              groups.update(3, `p₃`.+(1))
                              val `res₈`: Int = if (`p₃`.+(1).!=(`p₃`)) `self₃`(`p₃`.+(1)) else -1

                              (if (`res₈`.>=(0)) `res₈` else {
                                groups.update(3, `savedEnd₄`)
                                -1
                              }: Int)
                            }

                            (if (`res₇`.>=(0)) `res₇` else {
                              groups.update(5, `savedEnd₃`)
                              -1
                            }: Int)
                          } else -1

                          (if (`res₆`.>=(0)) `res₆` else {
                            groups.update(4, `savedStart₄`)
                            -1
                          }: Int)
                        }
                        if (`lp₅`.>=(0)) `lp₅` else {
                          val `savedStart₅`: Int = groups.apply(6)
                          groups.update(6, `p₃`)
                          val `res₉`: Int = if (`p₃`.<(`input₄`.length()).&&(`input₄`.charAt(`p₃`).==(98))) {
                            val `savedEnd₅`: Int = groups.apply(7)
                            groups.update(7, `p₃`.+(1))
                            val `res₁₀`: Int = {
                              val `savedEnd₆`: Int = groups.apply(3)
                              groups.update(3, `p₃`.+(1))
                              val `res₁₁`: Int = if (`p₃`.+(1).!=(`p₃`)) `self₃`(`p₃`.+(1)) else -1

                              (if (`res₁₁`.>=(0)) `res₁₁` else {
                                groups.update(3, `savedEnd₆`)
                                -1
                              }: Int)
                            }

                            (if (`res₁₀`.>=(0)) `res₁₀` else {
                              groups.update(7, `savedEnd₅`)
                              -1
                            }: Int)
                          } else -1

                          (if (`res₉`.>=(0)) `res₉` else {
                            groups.update(6, `savedStart₅`)
                            -1
                          }: Int)
                        }
                      }

                      (if (`res₅`.>=(0)) `res₅` else {
                        groups.update(2, `savedStart₃`)
                        -1
                      }: Int)
                    }
                    if (`step₃`.>=(0)) `step₃` else if (`p₃`.==(inputLen)) `p₃` else -1
                  }
                  `self₃`(0.+(1))
                }

                (if (`res₄`.>=(0)) `res₄` else {
                  groups.update(3, `savedEnd₂`)
                  -1
                }: Int)
              }

              (if (`res₃`.>=(0)) `res₃` else {
                groups.update(5, savedEnd)
                -1
              }: Int)
            } else -1

            (if (`res₂`.>=(0)) `res₂` else {
              groups.update(4, `savedStart₂`)
              -1
            }: Int)
          }
          if (`lp₄`.>=(0)) `lp₄` else {
            val `savedStart₆`: Int = groups.apply(6)
            groups.update(6, 0)
            val `res₁₂`: Int = if (0.<(`input₄`.length()).&&(`input₄`.charAt(0).==(98))) {
              val `savedEnd₇`: Int = groups.apply(7)
              groups.update(7, 0.+(1))
              val `res₁₃`: Int = {
                val `savedEnd₈`: Int = groups.apply(3)
                groups.update(3, 0.+(1))
                val `res₁₄`: Int = {
                  def `self₄`(`p₄`: Int): Int = {
                    val `step₄`: Int = {
                      val `savedStart₇`: Int = groups.apply(2)
                      groups.update(2, `p₄`)
                      val `res₁₅`: Int = {
                        val `lp₆`: Int = {
                          val `savedStart₈`: Int = groups.apply(4)
                          groups.update(4, `p₄`)
                          val `res₁₆`: Int = if (`p₄`.<(`input₄`.length()).&&(`input₄`.charAt(`p₄`).==(97))) {
                            val `savedEnd₉`: Int = groups.apply(5)
                            groups.update(5, `p₄`.+(1))
                            val `res₁₇`: Int = {
                              val `savedEnd₁₀`: Int = groups.apply(3)
                              groups.update(3, `p₄`.+(1))
                              val `res₁₈`: Int = if (`p₄`.+(1).!=(`p₄`)) `self₄`(`p₄`.+(1)) else -1

                              (if (`res₁₈`.>=(0)) `res₁₈` else {
                                groups.update(3, `savedEnd₁₀`)
                                -1
                              }: Int)
                            }

                            (if (`res₁₇`.>=(0)) `res₁₇` else {
                              groups.update(5, `savedEnd₉`)
                              -1
                            }: Int)
                          } else -1

                          (if (`res₁₆`.>=(0)) `res₁₆` else {
                            groups.update(4, `savedStart₈`)
                            -1
                          }: Int)
                        }
                        if (`lp₆`.>=(0)) `lp₆` else {
                          val `savedStart₉`: Int = groups.apply(6)
                          groups.update(6, `p₄`)
                          val `res₁₉`: Int = if (`p₄`.<(`input₄`.length()).&&(`input₄`.charAt(`p₄`).==(98))) {
                            val `savedEnd₁₁`: Int = groups.apply(7)
                            groups.update(7, `p₄`.+(1))
                            val `res₂₀`: Int = {
                              val `savedEnd₁₂`: Int = groups.apply(3)
                              groups.update(3, `p₄`.+(1))
                              val `res₂₁`: Int = if (`p₄`.+(1).!=(`p₄`)) `self₄`(`p₄`.+(1)) else -1

                              (if (`res₂₁`.>=(0)) `res₂₁` else {
                                groups.update(3, `savedEnd₁₂`)
                                -1
                              }: Int)
                            }

                            (if (`res₂₀`.>=(0)) `res₂₀` else {
                              groups.update(7, `savedEnd₁₁`)
                              -1
                            }: Int)
                          } else -1

                          (if (`res₁₉`.>=(0)) `res₁₉` else {
                            groups.update(6, `savedStart₉`)
                            -1
                          }: Int)
                        }
                      }

                      (if (`res₁₅`.>=(0)) `res₁₅` else {
                        groups.update(2, `savedStart₇`)
                        -1
                      }: Int)
                    }
                    if (`step₄`.>=(0)) `step₄` else if (`p₄`.==(inputLen)) `p₄` else -1
                  }
                  `self₄`(0.+(1))
                }

                (if (`res₁₄`.>=(0)) `res₁₄` else {
                  groups.update(3, `savedEnd₈`)
                  -1
                }: Int)
              }

              (if (`res₁₃`.>=(0)) `res₁₃` else {
                groups.update(7, `savedEnd₇`)
                -1
              }: Int)
            } else -1

            (if (`res₁₂`.>=(0)) `res₁₂` else {
              groups.update(6, `savedStart₆`)
              -1
            }: Int)
          }
        }

        (if (res.>=(0)) res else {
          groups.update(2, savedStart)
          -1
        }: Int)
      }
      if (`result₂`.==(inputLen)) {
        groups.update(1, `result₂`)
        Some.apply[Array[Int]](groups)
      } else None
    }).apply(`input₃`)
    override def findPrefixOf(source: CharSequence): Option[String] = {
      val matchPos: Int = ((startPos: Int, `input₅`: CharSequence) => {
        val `lp₇`: Int = if (startPos.<(`input₅`.length()).&&(`input₅`.charAt(startPos).==(97))) {
          def `self₅`(`p₅`: Int): Int = {
            val `step₅`: Int = {
              val `lp₈`: Int = if (`p₅`.<(`input₅`.length()).&&(`input₅`.charAt(`p₅`).==(97))) if (`p₅`.+(1).!=(`p₅`)) `self₅`(`p₅`.+(1)) else -1 else -1
              if (`lp₈`.>=(0)) `lp₈` else if (`p₅`.<(`input₅`.length()).&&(`input₅`.charAt(`p₅`).==(98))) if (`p₅`.+(1).!=(`p₅`)) `self₅`(`p₅`.+(1)) else -1 else -1
            }
            if (`step₅`.>=(0)) `step₅` else `p₅`
          }
          `self₅`(startPos.+(1))
        } else -1
        if (`lp₇`.>=(0)) `lp₇` else if (startPos.<(`input₅`.length()).&&(`input₅`.charAt(startPos).==(98))) {
          def `self₆`(`p₆`: Int): Int = {
            val `step₆`: Int = {
              val `lp₉`: Int = if (`p₆`.<(`input₅`.length()).&&(`input₅`.charAt(`p₆`).==(97))) if (`p₆`.+(1).!=(`p₆`)) `self₆`(`p₆`.+(1)) else -1 else -1
              if (`lp₉`.>=(0)) `lp₉` else if (`p₆`.<(`input₅`.length()).&&(`input₅`.charAt(`p₆`).==(98))) if (`p₆`.+(1).!=(`p₆`)) `self₆`(`p₆`.+(1)) else -1 else -1
            }
            if (`step₆`.>=(0)) `step₆` else `p₆`
          }
          `self₆`(startPos.+(1))
        } else -1
      }).apply(0, source)
      Option.when[String](matchPos.!=(-1))(source.subSequence(0, matchPos).toString())
    }
    override def findFirstIn(`source₂`: CharSequence): Option[String] = {
      val len: Int = `source₂`.length()
      var pos: Int = 0
      while (pos.<(len)) {
        val `matchPos₂`: Int = ((`startPos₂`: Int, `input₆`: CharSequence) => {
          val `lp₁₀`: Int = if (`startPos₂`.<(`input₆`.length()).&&(`input₆`.charAt(`startPos₂`).==(97))) {
            def `self₇`(`p₇`: Int): Int = {
              val `step₇`: Int = {
                val `lp₁₁`: Int = if (`p₇`.<(`input₆`.length()).&&(`input₆`.charAt(`p₇`).==(97))) if (`p₇`.+(1).!=(`p₇`)) `self₇`(`p₇`.+(1)) else -1 else -1
                if (`lp₁₁`.>=(0)) `lp₁₁` else if (`p₇`.<(`input₆`.length()).&&(`input₆`.charAt(`p₇`).==(98))) if (`p₇`.+(1).!=(`p₇`)) `self₇`(`p₇`.+(1)) else -1 else -1
              }
              if (`step₇`.>=(0)) `step₇` else `p₇`
            }
            `self₇`(`startPos₂`.+(1))
          } else -1
          if (`lp₁₀`.>=(0)) `lp₁₀` else if (`startPos₂`.<(`input₆`.length()).&&(`input₆`.charAt(`startPos₂`).==(98))) {
            def `self₈`(`p₈`: Int): Int = {
              val `step₈`: Int = {
                val `lp₁₂`: Int = if (`p₈`.<(`input₆`.length()).&&(`input₆`.charAt(`p₈`).==(97))) if (`p₈`.+(1).!=(`p₈`)) `self₈`(`p₈`.+(1)) else -1 else -1
                if (`lp₁₂`.>=(0)) `lp₁₂` else if (`p₈`.<(`input₆`.length()).&&(`input₆`.charAt(`p₈`).==(98))) if (`p₈`.+(1).!=(`p₈`)) `self₈`(`p₈`.+(1)) else -1 else -1
              }
              if (`step₈`.>=(0)) `step₈` else `p₈`
            }
            `self₈`(`startPos₂`.+(1))
          } else -1
        }).apply(pos, `source₂`)
        if (`matchPos₂`.!=(-1)) return Some.apply[String](`source₂`.subSequence(pos, `matchPos₂`).toString()) else ()
        pos = pos.+(1)
      }
      None
    }
    override def split(toSplit: CharSequence): Array[String] = {
      val `result₃`: ArrayBuffer[String] = scala.collection.mutable.ArrayBuffer.empty[String]
      val `len₂`: Int = toSplit.length()
      var `pos₂`: Int = 0
      var lastEnd: Int = 0
      while (`pos₂`.<(`len₂`)) {
        val matchEnd: Int = ((`startPos₃`: Int, `input₇`: CharSequence) => {
          val `lp₁₃`: Int = if (`startPos₃`.<(`input₇`.length()).&&(`input₇`.charAt(`startPos₃`).==(97))) {
            def `self₉`(`p₉`: Int): Int = {
              val `step₉`: Int = {
                val `lp₁₄`: Int = if (`p₉`.<(`input₇`.length()).&&(`input₇`.charAt(`p₉`).==(97))) if (`p₉`.+(1).!=(`p₉`)) `self₉`(`p₉`.+(1)) else -1 else -1
                if (`lp₁₄`.>=(0)) `lp₁₄` else if (`p₉`.<(`input₇`.length()).&&(`input₇`.charAt(`p₉`).==(98))) if (`p₉`.+(1).!=(`p₉`)) `self₉`(`p₉`.+(1)) else -1 else -1
              }
              if (`step₉`.>=(0)) `step₉` else `p₉`
            }
            `self₉`(`startPos₃`.+(1))
          } else -1
          if (`lp₁₃`.>=(0)) `lp₁₃` else if (`startPos₃`.<(`input₇`.length()).&&(`input₇`.charAt(`startPos₃`).==(98))) {
            def `self₁₀`(`p₁₀`: Int): Int = {
              val `step₁₀`: Int = {
                val `lp₁₅`: Int = if (`p₁₀`.<(`input₇`.length()).&&(`input₇`.charAt(`p₁₀`).==(97))) if (`p₁₀`.+(1).!=(`p₁₀`)) `self₁₀`(`p₁₀`.+(1)) else -1 else -1
                if (`lp₁₅`.>=(0)) `lp₁₅` else if (`p₁₀`.<(`input₇`.length()).&&(`input₇`.charAt(`p₁₀`).==(98))) if (`p₁₀`.+(1).!=(`p₁₀`)) `self₁₀`(`p₁₀`.+(1)) else -1 else -1
              }
              if (`step₁₀`.>=(0)) `step₁₀` else `p₁₀`
            }
            `self₁₀`(`startPos₃`.+(1))
          } else -1
        }).apply(`pos₂`, toSplit)
        if (matchEnd.!=(-1)) {
          `result₃`.+=(toSplit.subSequence(lastEnd, `pos₂`).toString())
          lastEnd = matchEnd
          `pos₂` = if (matchEnd.==(`pos₂`)) `pos₂`.+(1) else matchEnd
        } else `pos₂` = `pos₂`.+(1)
      }
      if (lastEnd.!=(`len₂`)) {
        `result₃`.+=(toSplit.subSequence(lastEnd, `len₂`).toString())
        ()
      } else ()
      `result₃`.toArray[String](ClassTag.apply[String](classOf[String]))
    }
    override def unapply(`input₈`: CharSequence): Option[Tuple2[String, Either[Either[String, String], Tuple2[String, String]]]] = $anon.this.matchesWithCaps(`input₈`).map[Tuple2[String, Either[Either[String, String], Tuple2[String, String]]]](((caps: Array[Int]) => {
      val hchain: HAppend[HSingleton[String], HSingleton[AltRep[[_$1 >: Nothing <: Any] =>> HSingleton[String], [_$1 >: Nothing <: Any] =>> HSingleton[String], true, [A >: Nothing <: Any, B >: Nothing <: Any] =>> Either[Either[A, B], Tuple2[A, B]]]]] = HAppend.apply[HSingleton[String], HSingleton[AltRep[[_$1 >: Nothing <: Any] =>> HSingleton[String], [_$1 >: Nothing <: Any] =>> HSingleton[String], true, [A >: Nothing <: Any, B >: Nothing <: Any] =>> Either[Either[A, B], Tuple2[A, B]]]]](HSingleton.apply[String](`input₈`.subSequence(caps.apply(2), caps.apply(3)).toString()), {
        val left: Option[Sanitised[HSingleton[String]]] = {
          val cap: Option[String] = {
            val l: Int = caps.apply(4)
            val u: Int = caps.apply(5)
            if (l.>=(0).&&(u.>=(0))) Some.apply[String](`input₈`.subSequence(l, u).toString()) else None
          }
          if (cap.isDefined) Some.apply[Sanitised[HSingleton[String]]](Sanitised.apply[HSingleton[String]](HSingleton.apply[String](cap.get), true)) else None
        }
        val right: Option[Sanitised[HSingleton[String]]] = {
          val `cap₂`: Option[String] = {
            val `l₂`: Int = caps.apply(6)
            val `u₂`: Int = caps.apply(7)
            if (`l₂`.>=(0).&&(`u₂`.>=(0))) Some.apply[String](`input₈`.subSequence(`l₂`, `u₂`).toString()) else None
          }
          if (`cap₂`.isDefined) Some.apply[Sanitised[HSingleton[String]]](Sanitised.apply[HSingleton[String]](HSingleton.apply[String](`cap₂`.get), true)) else None
        }
        if (left.isDefined.&&(right.isDefined)) Some.apply[Sanitised[HSingleton[Either[Either[HSingleton[String], HSingleton[String]], Tuple2[HSingleton[String], HSingleton[String]]]]]](Sanitised.apply[HSingleton[Either[Either[HSingleton[String], HSingleton[String]], Tuple2[HSingleton[String], HSingleton[String]]]]](HSingleton.apply[Either[Either[HSingleton[String], HSingleton[String]], Tuple2[HSingleton[String], HSingleton[String]]]](Right.apply[Nothing, Tuple2[HSingleton[String], HSingleton[String]]](Tuple2.apply[HSingleton[String], HSingleton[String]](left.get.captures, right.get.captures))), left.get.any.||(right.get.any))) else if (left.isDefined) Some.apply[Sanitised[HSingleton[Either[Either[HSingleton[String], Nothing], Tuple2[HSingleton[String], Nothing]]]]](Sanitised.apply[HSingleton[Either[Either[HSingleton[String], Nothing], Tuple2[HSingleton[String], Nothing]]]](HSingleton.apply[Either[Either[HSingleton[String], Nothing], Tuple2[HSingleton[String], Nothing]]](Left.apply[Left[HSingleton[String], Nothing], Nothing](Left.apply[HSingleton[String], Nothing](left.get.captures))), left.get.any)) else if (right.isDefined) Some.apply[Sanitised[HSingleton[Either[Either[Nothing, HSingleton[String]], Tuple2[Nothing, HSingleton[String]]]]]](Sanitised.apply[HSingleton[Either[Either[Nothing, HSingleton[String]], Tuple2[Nothing, HSingleton[String]]]]](HSingleton.apply[Either[Either[Nothing, HSingleton[String]], Tuple2[Nothing, HSingleton[String]]]](Left.apply[Right[Nothing, HSingleton[String]], Nothing](Right.apply[Nothing, HSingleton[String]](right.get.captures))), right.get.any)) else None
      }.get.captures)
      val node: HAppend[HSingleton[String], HSingleton[AltRep[[_$1 >: Nothing <: Any] =>> HSingleton[String], [_$1 >: Nothing <: Any] =>> HSingleton[String], true, [A >: Nothing <: Any, B >: Nothing <: Any] =>> Either[Either[A, B], Tuple2[A, B]]]]] = hchain
      Tuple2.apply[String, Either[Either[String, String], Tuple2[String, String]]](node.left.value, node.right.value match {
        case Left(Left(left)) =>
          Left.apply[Left[String, Nothing], Nothing](Left.apply[String, Nothing](`left₂`.value))
        case Left(Right(right)) =>
          Left.apply[Right[Nothing, String], Nothing](Right.apply[Nothing, String](`right₂`.value))
        case Right(Tuple2(left, right)) =>
          Right.apply[Nothing, Tuple2[String, String]](Tuple2.apply[String, String](`left₃`.value, `right₃`.value))
      })
    }))
  }

  (new $anon(): Regex[Tuple2[String, Either[Either[String, String], Tuple2[String, String]]]])
}